# ฟีเจอร์สร้างภาพแบนเนอร์ด้วย AI

## 📝 ภาพรวม

ฟีเจอร์นี้ช่วยให้ผู้ใช้สามารถสร้างภาพแบนเนอร์คุณภาพสูงด้วย AI โดยใช้ Replicate API กับโมเดล **Google Nano Banana Pro** 🍌 ซึ่งเป็นโมเดล text-to-image รุ่นล่าสุดจาก Google ที่ให้ผลลัพธ์คุณภาพสูงและรวดเร็ว

**ลิงก์อ้างอิง:** [Nano Banana Pro Examples](https://replicate.com/google/nano-banana-pro/examples)

## 🚀 คุณสมบัติ

- ✨ สร้างภาพแบนเนอร์จากคำอธิบาย (Text Prompt)
- 🎯 เลือกความละเอียด: **1K, 2K, 4K**
- 📐 รองรับสัดส่วนภาพหลากหลาย (Aspect Ratio):
  - 16:9 - Landscape Wide (เหมาะสำหรับ YouTube, Website)
  - 4:3 - Standard (เหมาะสำหรับ Presentation)
  - 1:1 - Square (เหมาะสำหรับ Instagram)
  - 3:4 - Portrait (เหมาะสำหรับ Story)
  - 9:16 - Vertical (เหมาะสำหรับ TikTok, Reels)
- 🖼️ รองรับรูปแบบไฟล์: **PNG, JPG, WebP**
- 🛡️ Safety Filter Level (block_only_high)
- 💾 บันทึกภาพที่สร้างลงในฐานข้อมูล
- ⬇️ ดาวน์โหลดภาพที่สร้างแล้ว
- 🔒 ระบบ Authentication ด้วย Supabase

## 🛠️ การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
npm install replicate
```

### 2. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` ในโฟลเดอร์ root ของโปรเจกต์:

```env
# Replicate API Token สำหรับสร้างภาพแบนเนอร์
REPLICATE_API_TOKEN=r8_O0Qgaqr2FxTDSVvevKjk2yLjxI4x6oE4AMDCT

# OpenRouter API Key (สำหรับสร้างคอนเทนต์)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Next.js App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

**หมายเหตุ:** API Token ที่ให้มาได้ถูกฝังไว้ในโค้ดแล้วเป็น fallback แต่แนะนำให้ใส่ใน environment variable เพื่อความปลอดภัย

### 3. รัน Database Migration

รันคำสั่งนี้เพื่อสร้างตารางในฐานข้อมูล Supabase:

```bash
# ใช้ Supabase CLI
supabase db push

# หรือรันผ่าน Supabase Dashboard
# คัดลอกเนื้อหาจาก supabase/migrations/002_create_content_and_banners.sql
# และรันใน SQL Editor
```

## 📂 โครงสร้างไฟล์

```
app/
├── api/
│   └── ai/
│       └── banner/
│           └── route.ts          # API endpoint สำหรับสร้างแบนเนอร์
└── dashboard/
    └── banner/
        └── page.tsx              # หน้า UI สำหรับสร้างแบนเนอร์

components/
└── dashboard/
    ├── sidebar.tsx               # เพิ่มเมนู "สร้างแบนเนอร์"
    └── mobile-sidebar.tsx        # เพิ่มเมนู "สร้างแบนเนอร์" (มือถือ)

supabase/
└── migrations/
    └── 002_create_content_and_banners.sql  # Migration สำหรับตาราง banners
```

## 🔧 การใช้งาน

### ผ่าน UI

1. เข้าสู่ระบบในแอปพลิเคชัน
2. คลิกที่เมนู "สร้างแบนเนอร์" ในแถบด้านข้าง
3. กรอกคำอธิบายภาพที่ต้องการ (Prompt) เป็นภาษาไทยหรือภาษาอังกฤษ
4. เลือกขนาดที่ต้องการ (หรือใช้ Preset)
5. กดปุ่ม "สร้างภาพแบนเนอร์"
6. รอประมาณ 10-30 วินาที
7. ดาวน์โหลดภาพที่สร้างแล้ว

### ผ่าน API

```typescript
// POST /api/ai/banner
const response = await fetch('/api/ai/banner', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    prompt: 'ภาพแบนเนอร์สำหรับร้านกาแฟ มีบรรยากาศอบอุ่น',
    resolution: '2K',
    aspect_ratio: '16:9',
    output_format: 'png',
    safety_filter_level: 'block_only_high',
  }),
})

const data = await response.json()
console.log(data.imageUrl) // URL ของภาพที่สร้าง
```

## 🎯 ตัวอย่าง Prompt ที่ดี

1. **ร้านอาหาร:**
   ```
   ภาพแบนเนอร์สำหรับร้านอาหารญี่ปุ่น มีซูชิสวยงามวางบนจานไม้ 
   ฉากหลังเป็นร้านสไตล์มินิมอล แสงสีอบอุ่น คุณภาพสูง
   ```

2. **อีคอมเมิร์ซ:**
   ```
   Banner for online fashion store, modern minimal style, 
   elegant clothing display, pastel colors, professional photography
   ```

3. **โปรโมชั่น:**
   ```
   Sale promotion banner, vibrant colors, modern gradient background,
   floating product elements, energetic and eye-catching
   ```

## 🗄️ Database Schema

### ตาราง `banners`

| Column       | Type      | Description                    |
|--------------|-----------|--------------------------------|
| id           | uuid      | Primary key                    |
| user_id      | uuid      | Foreign key to auth.users      |
| prompt       | text      | คำอธิบายที่ใช้สร้างภาพ          |
| image_url    | text      | URL ของภาพที่สร้าง             |
| resolution   | text      | ความละเอียด (1K, 2K, 4K)       |
| aspect_ratio | text      | สัดส่วนภาพ (16:9, 4:3, etc.)   |
| created_at   | timestamp | เวลาที่สร้าง                   |
| updated_at   | timestamp | เวลาที่อัปเดตล่าสุด             |

## 🔐 ความปลอดภัย

- ใช้ Row Level Security (RLS) ใน Supabase
- ผู้ใช้สามารถดูและจัดการเฉพาะแบนเนอร์ของตัวเองเท่านั้น
- ต้อง Authentication ก่อนใช้งาน API

## 💡 Tips

1. **เขียน Prompt ให้ละเอียด:** ยิ่งอธิบายมากเท่าไร ภาพที่ได้จะตรงกับที่ต้องการมากขึ้น
2. **ระบุสไตล์:** เช่น "minimal", "modern", "vintage", "professional"
3. **ระบุสี:** ช่วยให้ได้ tone and mood ที่ต้องการ
4. **ระบุคุณภาพ:** เช่น "high quality", "4K", "professional photography"

## 🚨 ข้อจำกัด

- ใช้เวลาในการสร้างภาพประมาณ 10-30 วินาที
- ความละเอียดที่รองรับ: 1K, 2K, 4K
- Replicate API มีค่าใช้จ่าย (ตรวจสอบที่ https://replicate.com/pricing)
- API Token ที่ให้มามีการจำกัดการใช้งาน
- รองรับสัดส่วนภาพ: 16:9, 4:3, 1:1, 3:4, 9:16

## 🐛 Troubleshooting

### ปัญหา: ไม่สามารถสร้างภาพได้

1. ตรวจสอบ Replicate API Token
2. ตรวจสอบ console log ใน browser และ terminal
3. ตรวจสอบว่า migration รันสำเร็จแล้ว

### ปัญหา: บันทึกในฐานข้อมูลไม่ได้

1. ตรวจสอบ Supabase connection
2. ตรวจสอบ RLS policies
3. รัน migration อีกครั้ง

## 📚 เอกสารเพิ่มเติม

- [Replicate Documentation](https://replicate.com/docs)
- [Nano Banana Pro Model](https://replicate.com/google/nano-banana-pro)
- [Nano Banana Pro Examples](https://replicate.com/google/nano-banana-pro/examples)
- [Supabase Documentation](https://supabase.com/docs)
- [Next.js Documentation](https://nextjs.org/docs)

## 🎉 สรุป

ฟีเจอร์นี้ช่วยให้ผู้ใช้สามารถสร้างภาพแบนเนอร์คุณภาพสูงได้อย่างง่ายดายผ่านการอธิบายด้วยข้อความ เหมาะสำหรับนักการตลาด Content Creator และผู้ที่ต้องการสร้างภาพโปรโมชั่นอย่างรวดเร็ว

